<article class="fl w-100 w-50-m w-50-l w-50-ns pa1 pa3-ns">
  <img class="fl w-100 pb3" src="{{project.data.hero }}" />

  {% if project.data.externalURL != null %}
  <a class="no-underline black dim" href="{{ project.data.externalURL | url }}">
    <div class="fl w-100">

    <h3 class="dib v-mid">{{project.data.title }} ↗</h3>    <p class="dib v-mid w-50">{{project.date | displayDateYear }}</p>

    {% else %}
    <a class="no-underline black dim" href="{{ project.url | url }}">
      <div class="fl w-100">
      <h3 class="fl dib v-mid">{{project.data.title }}</h3> <p class="fr dib v-mid">{{project.date | displayDateYear }}</p>
      {% endif %}
      <div  />
      <p class="fl w-100 preview-text">{{project.data.description }}</p>

      <br />
    </a>

</article>
