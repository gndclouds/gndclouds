<article class="fl w-100 w-50-m w-50-l w-50-ns pa1 pa3-ns">
  <img class="fl w-100 pb3" src="{{project.data.hero }}" />

  {% if project.data.externalURL != null %}
  <a class="no-underline black dim" href="{{ project.data.externalURL | url }}">
    <h3 class="dib v-mid w-50">{{project.data.title }} ↗</h3>    <p class="dib v-mid w-50">{{project.date | displayDateYear }}</p>

    {% else %}
    <a class="db pv4  ph0-l no-underline black dim" href="{{ project.url | url }}">
      <h3 class="dib v-mid w-50">{{project.data.title }}</h3> <p class="dib v-mid w-50">{{project.date | displayDateYear }}</p>
      {% endif %}
      <p class="w-100">{{project.data.description }}</p>

      <br />
    </a>

</article>
