{
  "version": 2,
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next"
    }
  ],
  "build": {
    "env": {
      "GIT_SUBMODULE_STRATEGY": "recursive"
    },
    "commands": {
      "build": "git submodule update --init --recursive && next build"
    }
  }
}
