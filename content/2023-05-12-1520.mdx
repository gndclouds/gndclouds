---
title: 2023-05-12-1520
publishedAt: "2023-05-12"
type: "Log"
tags: log/dev, log/hardware,
---

```tsx
<Link href={`/${encodeURIComponent(router.asPath)}`}>This link looks like</Link>
```

```tsx
<Link href={`/${encodeURIComponent(router.asPath)}`}>This link looks like</Link>
```

```tsx
<Link href={`/${encodeURIComponent(router.asPath)}`}>This link looks like</Link>
```

```tsx
<Link href={`/${encodeURIComponent(router.asPath)}`}>This link looks like</Link>
```

```tsx
<Link href={`/${encodeURIComponent(router.asPath)}`}>This link looks like</Link>
```
